
spring:
  profiles: test
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/usel?useSSL=false  
    username: root   
    password: root  

  jpa:
    hibernate:
      ddl-auto: validate
      show-sql: true
      dialect: org.hibernate.dialect.MySQLDialect
 
  flyway:
    validateOnMigrate: false
  
